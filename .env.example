# Required — your OpenAI API key (used server-side only, never sent to the browser)
OPENAI_API_KEY=sk-...

# Optional — YouTube channel ID to auto-load the latest episode on page load
# Get it from a channel's About page > Share > Copy channel ID
YOUTUBE_CHANNEL_ID=UCkkhmBWfS7pILYIk0izkc3A
